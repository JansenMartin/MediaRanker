require "test_helper"

describe VotesController do
  # it "must be a real test" do
  #   flunk "Need real tests"
  # end

  describe "create" do
    # It needs to add a vote to the database
    # It needs to reject a second vote made by the same user
    # It needs to accept only votes from logged-in users
    # It needs to add a vote to a particular work
    # It needs to add a vote to a particular user
  end
end
